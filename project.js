export default class Project {
  constructor(name, img, descripton, code, live) {
    (this.name = name),
      (this.img = img),
      (this.descripton = descripton),
      (this.code = code),
      (this.live = live);
  }
}
